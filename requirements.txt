# Runtime dependencies for analysis scripts and the Streamlit dashboard.
# Prefer: uv sync   (uses pyproject.toml + uv.lock)
# Or:     pip install -r requirements.txt
matplotlib>=3.11.0
pandas>=3.0.3
plotly>=6.8.0
seaborn>=0.13.2
streamlit>=1.59.1
